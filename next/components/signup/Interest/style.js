import styled from '@emotion/styled';

export const Select = styled.select`
  width: 100%;
  font-size: 1rem;
  padding: 0.5rem;
  outline: 0;
`;

export const Option = styled.option`
  outline: 0;
  padding: 0.5rem;
`;
